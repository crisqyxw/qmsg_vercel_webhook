{
  "name": "artalk-qmsg-webhook",
  "version": "1.1.0",
  "description": "Artalk评论通知转发到QMSG的Webhook服务",
  "main": "api/webhook.js",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "deploy": "vercel --prod",
    "build": "echo 'No build step required'"
  },
  "keywords": ["artalk", "qmsg", "webhook", "vercel", "dashboard"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.x"
  }
}